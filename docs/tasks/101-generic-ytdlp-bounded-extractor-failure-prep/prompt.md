# Session Bootstrap — GENERIC-YTDLP-BOUNDED-EXTRACTOR-FAILURE-PREP

Execute Issue #101 using the repository Worker protocol.

Claim only if live #101 is exactly:

```text
status:ready
env:cloud
no active owner
```

Read `AGENTS.md`, Issue #101 and its comments, `task.md`, lifecycle/freshness
protocols, #67 Attempt 8 blocker evidence, and accepted
#79/#83/#85/#95/#97/#99/R008 authorities before claim.

Frozen execution:

```text
Planning Base: 951e73d2f66d8f26a87535d61acd6c0dfd74800e
Attempt: 1
Environment: env:cloud
Real-site request: forbidden
```

Implement only the closed bounded worker/extractor failure taxonomy required by
`task.md`. Raw stderr, exception text, URLs, headers, bodies, signed query data
and credentials must never enter the worker envelope, smoke output, logs or
durable Evidence. Unknown/malformed envelopes and arbitrary nonzero exits remain
generic fail-closed failures.

Run J1–J4 on one exact Candidate, including native hosted ARM64 equivalence and
the full accepted sandbox/runtime/security regressions. Do not call Bilibili or
any public site, change the frozen runtime, weaken #95/R008, or execute #67.

Report C1–C8 explicitly. Then move to `status:review` or `status:blocked`,
release ownership and STOP. Worker must not merge, close #101 or start #67.
