# Session Bootstrap — R008 isolated browser slot research

You are starting a future Worker session for `liqiangcc/jellyfin-web-media-gateway` Issue #193.

```text
GitHub Issue: #193
Task Contract: docs/tasks/193-r008-isolated-browser-slot-research/task.md
Expected worker: cloud-codex
Expected environment: env:cloud
Downstream handoff profile: docs/tasks/handoffs/cloud.md
```

Before any action, read `AGENTS.md`, live Issue #193 and all comments, the Task Contract above, `docs/tasks/issue-lifecycle-protocol.md`, `docs/tasks/README.md`, `docs/tasks/execution-anchor-recovery-protocol.md`, `docs/tasks/freshness-integration-protocol.md`, all canonical documents required by `AGENTS.md`, and complete histories of Issues #188, #191, #157, #166, #182 and #185. Confirm that #193 is `status:ready`, unclaimed and eligible for `env:cloud`; while it is `status:draft`, do not claim or execute it.

The Task Contract is the only source for Goal, Scope, Claims, Success Criteria, Evidence and boundaries. Perform repository research first. A target check, if the live Issue authorizes it, is one bounded read-only authenticated Tailscale SSH capability/admission check only. Do not inspect existing `source-runtime` service/profile/proxy contents or state, and do not start, connect to, attach to, observe, reconfigure or terminate Chrome, VNC, CDP, X/Wayland, Node or any browser/session. Do not build, install, launch an artifact, access Bilibili, or mutate #188/#191/#157/#166/#182/#185.

Record the requested `gpt-5.6-luna` high-reasoning runtime and actual Fast availability. After a valid claim, follow the Issue lifecycle: start one Attempt, post `[EXECUTION REPORT]` or `[BLOCKER REPORT]`, set the corresponding review/blocked state, release ownership and stop. Do not provision an isolated slot or begin a #188/#191 Attempt; any design change or external owner/admin authorization is a Coordinator decision for a future Task.
