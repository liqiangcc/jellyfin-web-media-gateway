name: bilibili-browser-probe

on:
  pull_request:
    paths:
      - 'experiments/bilibili-browser-probe/**'
      - 'plugins/bilibili/**'
      - 'docs/research/bilibili-browser-probe-runbook.md'
      - '.github/workflows/bilibili-browser-probe.yml'
  workflow_dispatch:
    inputs:
      candidate_sha:
        description: 'Exact candidate commit to verify'
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: bilibili-browser-probe-${{ github.event.pull_request.head.sha || inputs.candidate_sha || github.sha }}
  cancel-in-progress: false

env:
  CANDIDATE_SHA: ${{ github.event.pull_request.head.sha || inputs.candidate_sha || github.sha }}

jobs:
  probe-contract:
    name: J1 probe contract and sanitized schema
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CANDIDATE_SHA }}
          persist-credentials: false
      - name: Assert exact Candidate SHA
        run: test "$(git rev-parse HEAD)" = "$CANDIDATE_SHA"
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install locked runtime with scripts disabled
        run: |
          runtime="$RUNNER_TEMP/probe-runtime"
          mkdir -p "$runtime"
          cp experiments/bilibili-browser-probe/package.json experiments/bilibili-browser-probe/package-lock.json "$runtime/"
          npm ci --ignore-scripts --no-audit --no-fund --prefix "$runtime"
          test "$(node -p "require('$runtime/node_modules/playwright-core/package.json').version")" = 1.55.0
      - name: Run synthetic contract and leakage negatives
        env:
          PLAYWRIGHT_RUNTIME_ROOT: ${{ runner.temp }}/probe-runtime
        run: node --test experiments/bilibili-browser-probe/contract.test.mjs experiments/bilibili-browser-probe/diagnostic.test.mjs experiments/bilibili-browser-probe/transport.test.mjs experiments/bilibili-browser-probe/finalizer.test.mjs experiments/bilibili-browser-probe/artifact.test.mjs

  probe-containment:
    name: J2 Chromium broker containment and cleanup
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CANDIDATE_SHA }}
          persist-credentials: false
      - name: Assert exact Candidate SHA
        run: test "$(git rev-parse HEAD)" = "$CANDIDATE_SHA"
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install locked runtime with scripts disabled
        run: |
          runtime="$RUNNER_TEMP/probe-runtime"
          mkdir -p "$runtime"
          cp experiments/bilibili-browser-probe/package.json experiments/bilibili-browser-probe/package-lock.json "$runtime/"
          npm ci --ignore-scripts --no-audit --no-fund --prefix "$runtime"
          cp -a "$runtime/node_modules" "$GITHUB_WORKSPACE/node_modules"
      - uses: browser-actions/setup-chrome@v2
      - name: Discover hosted Chromium
        shell: bash
        run: |
          for candidate in google-chrome-stable google-chrome chromium chromium-browser; do
            if command -v "$candidate" >/dev/null 2>&1; then
              browser_path="$(command -v "$candidate")"
              CHROME_PATH="$browser_path"
              echo "CHROME_PATH=$CHROME_PATH" >> "$GITHUB_ENV"
              "$CHROME_PATH" --version
              break
            fi
          done
          if [ -z "${CHROME_PATH:-}" ]; then
            echo "no external system Chrome found" >&2
            exit 1
          fi
          test -n "${CHROME_PATH:-}"
      - name: Run real Chromium synthetic containment probe
        run: node experiments/bilibili-browser-probe/probe.mjs > probe-evidence.json
      - name: Run deterministic live broker and consumer harness
        run: set -o pipefail; node --test experiments/bilibili-browser-probe/live-broker.test.mjs | tee live-broker-evidence.txt
      - uses: actions/upload-artifact@v4
        with:
          name: bilibili-browser-live-broker-evidence-${{ env.CANDIDATE_SHA }}
          path: live-broker-evidence.txt
          if-no-files-found: error
          retention-days: 14
      - name: Assert bounded sanitized evidence
        shell: bash
        run: |
          test -s probe-evidence.json
          node -e 'const e=require("./probe-evidence.json"); const c=e.observation.containment; if(e.observation.part_match!==true || c.broker!=="loopback-allowlist") process.exit(1); if(c.redirect!=="denied" || c.worker!=="denied" || !c.websocket.startsWith("denied")) process.exit(1); if(e.observation.cleanup.browser_exit!=="complete") process.exit(1);'
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const evidence = fs.readFileSync('probe-evidence.json', 'utf8');
          if (/https?:\/\/|blob:|file:|cookie:|authorization:|signed-url|sessdata|bearer|BV[0-9A-Za-z]{10,}/i.test(evidence)) throw new Error('sanitized evidence leaked a URL, selector, or secret marker');
          NODE
      - uses: actions/upload-artifact@v4
        with:
          name: bilibili-browser-probe-evidence-${{ env.CANDIDATE_SHA }}
          path: probe-evidence.json
          if-no-files-found: error
          retention-days: 14

  probe-artifact-build:
    name: J3a build manifest-addressed artifact
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CANDIDATE_SHA }}
          persist-credentials: false
      - name: Assert exact Candidate SHA
        run: test "$(git rev-parse HEAD)" = "$CANDIDATE_SHA"
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install locked runtime with scripts disabled
        run: |
          runtime="$RUNNER_TEMP/probe-runtime"
          mkdir -p "$runtime"
          cp experiments/bilibili-browser-probe/package.json experiments/bilibili-browser-probe/package-lock.json "$runtime/"
          npm ci --ignore-scripts --no-audit --no-fund --prefix "$runtime"
      - uses: browser-actions/setup-chrome@v2
      - name: Discover hosted Chromium
        shell: bash
        run: |
          for candidate in google-chrome-stable google-chrome chromium chromium-browser; do
            if command -v "$candidate" >/dev/null 2>&1; then
              browser_path="$(command -v "$candidate")"
              CHROME_PATH="$browser_path"
              echo "BROWSER_VERSION=$($CHROME_PATH --version | head -1)" >> "$GITHUB_ENV"
              echo "CHROME_PATH=$CHROME_PATH" >> "$GITHUB_ENV"
              break
            fi
          done
          if [ -z "${CHROME_PATH:-}" ]; then
            echo "no external system Chrome found" >&2
            exit 1
          fi
          test -n "${CHROME_PATH:-}"
      - name: Build and verify manifest-addressed artifact
        run: |
          CANDIDATE_SHA="$CANDIDATE_SHA" BROWSER_VERSION="$BROWSER_VERSION" PLAYWRIGHT_RUNTIME_ROOT="$RUNNER_TEMP/probe-runtime" node experiments/bilibili-browser-probe/artifact.mjs build "$RUNNER_TEMP/probe-artifact"
          CANDIDATE_SHA="$CANDIDATE_SHA" node experiments/bilibili-browser-probe/artifact.mjs verify "$RUNNER_TEMP/probe-artifact"
          tar -C "$RUNNER_TEMP" -czf "$RUNNER_TEMP/bilibili-browser-probe.tar.gz" probe-artifact
          sha256sum "$RUNNER_TEMP/bilibili-browser-probe.tar.gz" | tee "$RUNNER_TEMP/archive.sha256"
      - uses: actions/upload-artifact@v4
        with:
          name: bilibili-browser-probe-${{ env.CANDIDATE_SHA }}
          path: |
            ${{ runner.temp }}/probe-artifact
            ${{ runner.temp }}/bilibili-browser-probe.tar.gz
            ${{ runner.temp }}/archive.sha256
          if-no-files-found: error
          retention-days: 14
  probe-artifact-consumer:
    name: J3 downloaded artifact independent consumer
    needs: probe-artifact-build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CANDIDATE_SHA }}
          persist-credentials: false
      - name: Assert exact Candidate SHA
        run: test "$(git rev-parse HEAD)" = "$CANDIDATE_SHA"
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: browser-actions/setup-chrome@v2
      - name: Download artifact produced by J3a
        uses: actions/download-artifact@v4
        with:
          name: bilibili-browser-probe-${{ env.CANDIDATE_SHA }}
          path: ${{ runner.temp }}/downloaded
      - name: Verify downloaded manifest and run independent consumer
        shell: bash
        run: |
          bundle="$RUNNER_TEMP/downloaded/probe-artifact"
          test -f "$bundle/manifest.json"
          CANDIDATE_SHA="$CANDIDATE_SHA" node "$bundle/experiments/bilibili-browser-probe/artifact.mjs" verify "$bundle"
          browser_path=""
          for candidate in google-chrome-stable google-chrome chromium chromium-browser; do
            if command -v "$candidate" >/dev/null 2>&1; then browser_path="$(command -v "$candidate")"; break; fi
          done
          echo "resolved_browser_path=${browser_path:-none}"
          test -x "$browser_path"
          CANDIDATE_SHA="$CANDIDATE_SHA" CHROME_PATH="$browser_path" node "$bundle/experiments/bilibili-browser-probe/consumer.mjs" "$bundle"

  probe-static-boundary:
    name: J4 live admission and static boundary
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CANDIDATE_SHA }}
          persist-credentials: false
      - name: Assert exact Candidate SHA
        run: test "$(git rev-parse HEAD)" = "$CANDIDATE_SHA"
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Verify live entry and boundary invariants
        shell: bash
        run: |
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          import { parseSelector, validateLiveInvocation } from './plugins/bilibili/live_selector.mjs';
          const selector = 'bilibili:BV14V411W7r5:part-2';
          const admission = validateLiveInvocation({ mode: 'live', selector });
          if (admission.navigation.host !== 'www.bilibili.com' || !admission.navigation.url.includes('/video/BV14V411W7r5?p=2')) throw new Error('plugin navigation descriptor mismatch');
          for (const value of ['https://www.bilibili.com/video/BV14V411W7r5', 'bilibili:BV14V411W7r5:part-2?cookie=x']) {
            try { parseSelector(value); throw new Error('caller authority accepted'); } catch (error) { if (error.message === 'caller authority accepted') throw error; }
          }
          const live = fs.readFileSync('experiments/bilibili-browser-probe/live.mjs', 'utf8');
          for (const marker of ['publicAddressFor', 'MAX_REQUESTS', 'MAX_INDEPENDENT_BYTES', 'serviceWorkers', 'launchPersistentContext', 'mkdtemp', 'pendingObservations', 'onTunnelClosed', 'transportFinalized', 'candidates.clear']) {
            if (!live.includes(marker)) throw new Error(`missing live boundary marker: ${marker}`);
          }
          const finalizer = fs.readFileSync('experiments/bilibili-browser-probe/finalizer.mjs', 'utf8');
          for (const marker of ['createFinalizer', 'finalizeResult', 'terminationClass', 'ephemeral_candidates', 'dns_pins']) {
            if (!finalizer.includes(marker)) throw new Error(`missing finalizer boundary marker: ${marker}`);
          }
          const preflight = fs.readFileSync('experiments/bilibili-browser-probe/transport-preflight.mjs', 'utf8');
          for (const marker of ['PREFLIGHT_AUTHORITY', 'method: \'CONNECT\'', 'page_navigation: false', 'media_request: false', 'selector: false', 'consumer: false']) {
            if (!preflight.includes(marker)) throw new Error(`missing transport preflight boundary marker: ${marker}`);
          }
          if (/process\.argv\[(?:2|3)\]|process\.env\.(URL|HOST|PORT|PROXY|COOKIE|AUTHORIZATION)/i.test(preflight)) throw new Error('caller-controlled transport authority found');
          NODE
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          import path from 'node:path';
          const roots = ['experiments/bilibili-browser-probe', 'plugins/bilibili'];
          const files = [];
          const visit = (root) => { for (const name of fs.readdirSync(root)) { const file = path.join(root, name); const stat = fs.statSync(file); if (stat.isDirectory()) visit(file); else if (file.endsWith('.mjs')) files.push(file); } };
          roots.forEach(visit);
          const text = files.map((file) => fs.readFileSync(file, 'utf8')).join('\n');
          if (/remote-debugging|user-data-dir|process\.env\.(COOKIE|AUTHORIZATION|CDP|PROXY)/i.test(text)) throw new Error('caller-controlled browser authority found');
          if (/gateway[-_](core|playback)|DisplayAdapter|PlaybackSession/.test(text)) throw new Error('production playback coupling found');
          NODE

  probe-integration:
    name: JI1 remote workspace integration and regressions
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CANDIDATE_SHA }}
          persist-credentials: false
      - name: Assert exact Candidate SHA
        run: test "$(git rev-parse HEAD)" = "$CANDIDATE_SHA"
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: rustfmt, clippy
      - name: Run exact Candidate integration checks
        run: |
          cargo fmt --all -- --check
          cargo clippy --workspace --all-targets -- -D warnings
          cargo test --workspace --all-targets
