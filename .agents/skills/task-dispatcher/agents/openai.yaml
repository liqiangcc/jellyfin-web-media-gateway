interface:
  display_name: "Task Dispatcher"
  short_description: "Dispatch Codex tasks through issue-linked tmux sessions"
  default_prompt: "Use $task-dispatcher to dispatch the provided Issue task through an issue-linked tmux Codex session."
policy:
  allow_implicit_invocation: false
