interface:
  display_name: "Task Dispatcher"
  short_description: "Dispatch and track issue-linked Codex tmux workers"
  default_prompt: "Use $task-dispatcher to dispatch or track the provided Issue through an isolated issue-linked tmux Codex worktree."
policy:
  allow_implicit_invocation: false
